{% extends 'base.html.twig' %}

{% block body %}
    <main class="center">
        <h1>Product creation</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Create" />
        {{ form_end(form) }}

        <ul>
            <li>
                <a href="{{ path('annonces') }}">Back to the list</a>
            </li>
        </ul>
    </main>
{% endblock %}
